# Improvement Ledger

Running record of product-improvement ideas mined from **actual usage**. Written by the weekly
usage-review routine and by any session that acts on one of its entries.

Long-lived by design: like `refactor-ledger.md`, this file is not deleted when a piece of work
finishes — entries change status instead. IDs are stable and never reused.

**Read this file before proposing anything.** Its main job is not to remember good ideas; it is to
remember the *rejected* ones. An autonomous weekly reviewer that cannot see what was already turned
down will propose the same three things forever, and the first thing to erode is your trust in it.

## Rules for entries

- **Evidence or it doesn't go in.** Every proposal names the signal or interaction pattern behind it,
  with counts. `dictationRestart ×7 in 5 days` is an entry; "the UI could be nicer" is not.
- **Max 3 new entries per run.** A list nobody reads is the same as no list.
- **Rejections keep their reason.** Status `rejected` without a `Why not` note is not a rejection,
  it is an invitation to re-propose.
- **Shipped entries get measured.** Once `plans/usage-metrics.csv` exists (outcome-signals Slice 3),
  a `shipped` entry moves to `measured` only when the metric it was supposed to move actually moved —
  or is honestly marked as having not moved.

Status values: `proposed` · `accepted` · `shipped` · `measured` · `rejected` · `superseded`.

## Ideas

| ID  | Idea | Evidence | Status | Run | Notes |
| --- | ---- | -------- | ------ | --- | ----- |
| I2 | `[code]` Investigate why transcription gets stuck in the `transcribing` phase for 3.5–4 minutes before the user gives up and manually cancels — check for a missing/too-long network timeout on the OpenAI GPT Transcribe request path | 2 `cancelledWhileProcessing` signals in a 4-day window (2026-08-10 and 2026-08-12), both `mode: transcription`, both `detail.phase: "transcribing"`, `gapMs` 211598 and 234394 (3.5min / 3.9min) between the interaction starting and the user cancelling. GPT Transcribe was the sole transcription model active all week (567/567 interactions), so this isn't a model-switch artifact. | shipped | 2 | Both cancels happened in otherwise-normal sessions (no error-log entry at either exact timestamp), consistent with a hung/very slow round-trip rather than a server error response. Rate is low (~0.35% of transcription attempts) but severity is high — several minutes of unresponsive UI before the user has to notice and intervene manually. **Shipped 2026-08-18 via the autonomous implementer (PR #45).** Root cause was not the provider: `request.timeoutInterval = 300s` overrode the session's 60s timer, and URLSession timers do not fire reliably on reused HTTP/2 connections. Fix is a wall-clock `NetworkDeadline` (60s) mapping to the existing `.requestTimeout` popup, plus a new `requestTimedOut` outcome signal so this falsifier is measurable at all. **Two caveats from the review, for whoever grades this:** (1) the 60s now covers upload time, so a ~20 MB single-shot LPCM upload needs ~2.7 Mbps sustained uplink — narrow exposure (slow uplink AND multi-minute uninterrupted speech) and non-destructive, since `.requestTimeout` is retryable and the audio is retained; the build agent's own notes wrongly called that case a previous *hang*, when it would previously have *succeeded*. (2) Live-meeting chunks share `SpeechService.transcribe`, so they emit `requestTimedOut` with `mode="transcription"` too — separate them by `detail.logPrefix` before reading them as dictation stalls. **Graded 2026-09-14 (run 5): the deadline is confirmed live — 3 `requestTimedOut` (60 s), each followed by a successful paste within 5–40 s. The cancel clause of the falsifier is ungradeable by construction (gapMs is measured from the previous completed dictation, 3.05 h in one case) — see I8.** |
| I3 | `[investigate]` `TranscriptionError.noSpeechDetected` ("error 21" in `errors-*.log`) fired 11 times across all 4 days of the window — every day had at least one. Understand whether these are expected (accidental/very brief mic triggers) or point to over-eager recording starts, and whether the user should get feedback beyond a silent failure | 11 occurrences in `errors-2026-08-10.log` (6), `errors-2026-08-11.log` (1, live-meeting segment), `errors-2026-08-12.log` (3), `errors-2026-08-13.log` (1) — confirmed via empirical Swift NSError-code bridging test that `TranscriptionError error 21` maps to `.noSpeechDetected`, not `.textTooShort` (`TextProcessingUtility.swift:403` is dead code today since `AppConstants.minimumTextLength == 1` makes it unreachable — the empty-string check fires first). Not visible in the outcome-signal stream at all: a `noSpeechDetected` failure produces no interaction record, so it cannot emit `pasted` or `dictationRestart` — a blind spot in the signal design worth noting. | proposed (scoped 2026-08-20) | 2 | Only 1 of the 690 interactions this week directly evidences a related restart (the "Ähm." filler-word case, single anecdote, not counted toward the bar). The 11 errors themselves clear the ≥2 bar on their own; root cause and user-facing severity still need a read of `SpeechErrorFormatter.swift`'s handling before proposing a fix — flagged as `[investigate]`, not yet a concrete fix. **Investigated 2026-08-20 (no code change yet) — two of the premises above are wrong.** (1) It is *not* a silent failure: `MenuBarController.handleProcessingError` (`MenuBarController.swift:1434`) already special-cases `.noSpeechDetected` into a brief info popup ("No speech was detected in your recording…"), deliberately not the persistent error popup. (2) It is *not* an over-eager recording start in the dead-silence sense: `MenuBarController.swift:2432` already gates every cloud-backed path on `lastRecordingWasSilent` (peak < -45 dB over the whole recording) and skips the API call entirely. **So every occurrence that reaches the API is a recording whose peak EXCEEDED -45 dB but which still transcribed to empty** — audible non-speech (keyboard, cough, room noise), not silence. **Wider count than the original window:** 39 occurrences across all `errors-*.log` (2026-07-22…2026-08-20, still firing), splitting 30 dictation-path / 9 `MEETING-SEGMENT`; the meeting ones are expected (a silent meeting chunk is normal) and must be excluded, the same `logPrefix` confound I2 hit. Recent rate is low: 2 dictation-path occurrences against 366 transcription interactions over 2026-08-14…08-20 (~0.5%). **The blocking problem is measurement, not behaviour:** 39 error-log entries vs **0** matching records in `interactions-*.jsonl` — `noSpeechDetected` emits no `OutcomeSignal` at all, so no fix to it can be graded. The tempting behavioural fix (replace the peak-power gate with a sustained-energy one, since a single transient defeats a peak gate) is **deliberately not proposed yet**: the discriminating data (peak dB and duration of the recordings that then failed) is not retained anywhere, and `MenuBarController.swift:2434` warns that tightening this gate "has caused real recordings to be dropped on low-gain mics". Instrument first, then decide — instrumentation gap #8, queue row #2 (HOLD). |
| I1 | `[code]` Move `ChunkedDictateRecorder`'s `AVAudioRecorder.record()`/`.stop()` off the main thread — `beginSession()` (start) and `rotateChunk()` (mid-recording rotation) call them synchronously, and both are the culprit in a captured hang | 2 watchdog hangs this week, both ≥4s, both with `ChunkedDictateRecorder` on top of the main-thread stack: `hang-20260728-165723.txt` (`rotateChunk` → `AVAudioRecorder.stop()` → `AQ::API::Queue::AwaitAllPendingCallbacks`) and `hang-20260802-170842.txt` (`beginSession`/`startChunkRecording` → `AVAudioRecorder.record()` → `AudioQueueXPC_Bridge::Start`). Same root cause both times. | shipped | 2026-08-03 | Not in the rejected table. Fits `analyze-chat-freeze`'s "main-thread hang" pattern but is a dictation-recording hang, not a chat one — new variant. Shipped 2026-08-03: `record()`/`stop()` now run on a serial `audioQueue`; state is committed on main first, `onChunkFinalized` waits for `stop()` so the WAV is closed before it is read. Same fix applied to `LiveMeetingRecorder` in the same pass — identical defect (main-thread `record()`/`stop()`, `didFinishChunk` firing before the WAV was closed) in a live path, found by structural sweep, **no captured hang of its own**; meetings rotate far more often than dictations so exposure is higher. `AudioRecorder` has the defect too but is the dead branch of `useChunkedDictateRecorder = true` — deliberately left alone. Verify by absence — no new `hang-*.txt` naming either recorder in the next review window. |
| I4 | `[logging]` `noSpeechDetected` cannot be attributed to a caller: thread a caller-supplied origin (`streamingChunk`/`dictation`/`meetingSegment`/`promptHistory`) plus the audio duration into the two `apiEmptyResult` emit sites, and tag or suppress streaming-chunk empties | 182 `noSpeechDetected` signals in the 6 days the instrumentation has been live (2026-09-02 onward), against 593 logged transcriptions — an apparent 23.5% failure rate. 139 of them carry `detail.logPrefix: "OPENAI-TRANSCRIPTION"`, a provider constant (`SpeechService.swift:1745`), not a mode. On 2026-09-06 their inter-arrival gaps cluster at 10.4–11.3s (57 of 88 gaps under 15s), which is exactly `AppConstants.dictateChunkMinDuration` (10.0) + `dictateChunkSilenceDuration` (1.0) = 11.0s, the `ChunkedDictateRecorder` rotation period; `DictateStreamingSession.swift:151` skips those chunks and the dictation still completes and pastes. Separately, 177 of 182 records log `peakDb: "n/a"` and `durationMs: "n/a"` — only the 5 `localSilenceGate` records carry real values, because `SpeechService.swift:2156` and `:2169` hardcode the strings. | proposed | 4 | Ranked first per the instrumentation-gaps rule: gap #8 is BUILT but produces no gradeable data, so **I3 remains unfalsifiable after two windows**. Downgraded gap #8 to PARTIAL and opened gap #9 for this. Blast radius is one logged field plus one parameter on an internal emit site. Falsifier: in the next window every `noSpeechDetected` record carries a non-`n/a` origin and duration, and the daily count stops tracking dictation length. |
| I6 | `[code]` An interrupted offline-model download still passes the completeness check and dead-ends minutes later — `ModelManager.findFile` only tests that a directory *named* the component exists, not that it holds a compiled model; and the local transcription path has no deadline at all | Two load failures, two different models, two days, both `"Unable to load model … Compile the model with Xcode"` → `"Model appears to be missing or incomplete"`: 2026-08-31 18:00:58 (`large-v3-turbo`, `TextDecoderContextPrefill.mlmodelc`) and 2026-09-03 12:55:47 (`whisper-small`, `MelSpectrogram.mlmodelc`). The second is after `9a3895e` (2026-09-02 11:16) added `requiredComponents`, which *does* list `MelSpectrogram.mlmodelc` — so the check ran and passed. Three `cancelledWhileProcessing` match LOCAL-SPEECH failures to the second (errors log CEST vs signals UTC): 09-03T10:56:17.985Z gapMs 36691, 09-03T11:25:26.949Z gapMs 486771 (8.1 min), 08-31T16:07:20.523Z gapMs 313658 (5.2 min). `grep 'NetworkDeadline\|deadline\|Timeout' LocalSpeechService.swift` returns nothing — I2's 60s deadline is network-only. Behavioural: offline models were 24/121, 43/94 and 27/117 of daily transcriptions on 08-31/09-02/09-03, then **0** across 09-04→09-07. | proposed | 4 | 10 `cancelledWhileProcessing` this week vs 2 in run 2's window; the multi-minute ones all resolve to this path, which is why I2's fix is neither confirmed nor falsified (0 `requestTimedOut` all week). Rhymes with the App Store vanishing-keys case, same offline-model dead end. Falsifier: next window has no LOCAL-SPEECH `"missing or incomplete"` line in `errors-*.log` and no `cancelledWhileProcessing` with `gapMs > 60000` within 5 min of one. |
| I7 | `[investigate]` A plausibility/glossary-echo discard on a streaming chunk is silently dropped instead of falling back to single-shot — the guards return `""`, `validateSpeechText` turns that into `noSpeechDetected`, and `DictateStreamingSession.swift:151` skips the chunk rather than taking the `return nil` re-transcribe branch one line below | `discardingGlossaryEchoTranscript` fired 9× on 2026-08-31, every one on 10.8–11.6s of audio (the 11.0s chunk period), plus `discardingImplausibleTranscript` 2× on 2026-09-01 (424 chars from 0.5s audio; 349 chars from 1.3s). All 11 on `gemini-3.1-flash-lite`, which went 153 transcriptions on 09-01 → 1 on 09-02 → 0 thereafter. Neither discard path emits an `OutcomeSignal`, so none of this is visible in the signal stream. | proposed | 4 | Filed `[investigate]`, not a fix: the "these were streaming chunks" step is inferred from durations, not observed (the unified log was unreadable from the sandbox this run). The guards themselves are correct — the model was confabulating. The remedy also touches the model lineup, and per the Pareto rule Flash-Lite is cheapest and not dominated, so it must not simply be pruned. Needs a human read of the call site before it builds. |
| I8 | `[logging]` `cancelledWhileProcessing` and `requestTimedOut` cannot say how long the user waited: `refTs`/`gapMs` point at the *previous completed* interaction (`lastEntryTs[mode]`), not at the stalled one — add `detail.processingMs` (elapsed since recording stopped / processing began) and, on the streaming path, `detail.pendingChunks`, stamped from a stop marker next to the existing `noteDictationStart` | 5 `cancelledWhileProcessing` this week, all `mode: transcription`, `phase: transcribing`, gapMs 419 569 / 550 831 / 591 154 / 1 316 911 / **10 982 545 (3.05 h)** — the last one proves gapMs is not a wait. 3 `requestTimedOut` carry gapMs 470 313–1 501 001 against `timeoutSeconds: "60"`. So I2's falsifier (`gapMs > 120000`, queue row 1) has never been gradeable, including run 2's 211 s / 234 s. `MenuBarController.swift:1319` passes no `refTs`; `DictateStreamingSession.swift:149` awaits chunk tasks with no session-level deadline, `:95` marks them `cancellable: false`. | proposed | 5 | Instrumentation gap #10. Positive side of the same grade: the NetworkDeadline fired 3× this week (first ever), each matched to an `errors-*.log` `stalled round-trip aborted after 60s` line and each followed by a successful paste 5–40 s later — I2's mechanism works; what is unmeasured is whether the 5 cancels happen inside the 60 s window (impatience / UI) or on a path the deadline does not cover. Falsifier: on 2026-09-21 every `cancelledWhileProcessing` / `requestTimedOut` record in the window carries a numeric `detail.processingMs`. |
| I9 | `[investigate]` A streaming dictation whose every chunk returns empty gives the user no feedback for the whole recording — the pill shows a live dictation while `DictateStreamingSession.swift:151` discards chunk after chunk, and the user only learns of it when nothing pastes | 2 of 44 empty-chunk episodes this week end in a `cancelledWhileProcessing` instead of a paste: 2026-09-07 07:30:09Z–07:32:21Z, **11 consecutive `apiEmptyResult` at 10.95 s median cadence (the 11.0 s chunk period), 132 s of recording**, cancel 1.7 s after the last empty, no interaction record; 2026-09-08 16:06:51Z–16:07:00Z, 2 empties, cancel 1.1 s later. The 09-07 episode sits in the same morning as an `invalidRequest` error and both NetworkDeadline aborts, so the provider was degraded; mic vs provider cannot be separated because `peakDb`/`durationMs` are `"n/a"` on all 80 records (gap #9). | proposed | 5 | Blocked on I4 / queue row 5 — second consecutive window in which that unbuilt row would have decided a finding. Fix candidates (a pill warning after N consecutive empty chunks; single-shot fallback at stop instead of joining N empties) are not chosen until gap #9 says whether the audio was silent. Not I5 (rejected): touches no prompt presets. Falsifier: after gap #9 is live, the next window's cancel-terminated empty-chunk episodes carry `durationMs`/`peakDb`; if the peaks are above the -45 dB gate the provider returned empty for audible input and the fallback path is the fix, otherwise it is an input-device warning. |
| I10 | `[code]` Read Aloud gaps between chunks: stream Gemini TTS over `streamGenerateContent?alt=sse` through the playback gate, ramp chunk caps 120 → 180 → 260 (Gemini) / 500 (OpenAI, xAI), 45 s / 30 s stall watchdog, buffering spinner on the pill | Owner report 2026-09-18 ("beim Vorlesen entstehen stets Pausen"). Log 2026-09-18 12:24, 900 chars, 1.5×: chunk 0 (107 chars) done at 5.8 s, chunk 1 (476 chars, 30.5 s audio) at 18.8 s → 7.5 s of silence, 0 errors. Live probes: streaming gives first audio in 0.9–3.3 s at ≈3.4× realtime, but the preview model pauses ~15 s at the ~17 s audio mark on inputs > ~300 chars (3 samples, Charon, German) and sometimes hangs outright | shipped | — | Shipped 2026-09-19 on `feat/read-aloud-gemini-streaming` (`ea601af`, interactive session). Baseline run 2026-09-19 13:17: 2045 chars, 11 chunks, first sound 0.96 s (was 6.5 s), synthesis 9.5 s, 0 stalls, 0 retries, 0 buffering events, `Playback completed (2:37)`. **Falsifier:** the 260-char Gemini cap and the 30 s stall budget are fitted, not documented — over the next 30 days of Read Aloud on Gemini, `TTS-PLAYBACK` buffering events (`onBufferingChanged`, pill "Loading…") on ≥ 20 % of runs with ≥ 4 chunks, or any `TTS-GEMINI-STREAM: … aborting the stalled stream` on a run that would have finished within the budget, falsifies the cap/budget pair. `TOO EARLY` until a release carrying `ea601af` is live for the operator's own build. **Filed as I7 on 2026-09-18 against origin/main, which lacked usage-review I7 (09-07, uncommitted until 2026-09-19); renumbered I10 at the rebase — the L10 case.** |

## Rejected — do not re-propose

| ID  | Idea | Why not | Run |
| --- | ---- | ------- | --- |
| I5 | One-tap Correct / Format / Rephrase menu items for Dictate Prompt | Owner rejected 2026-09-04: never separately approved (shipped inside the Sept-2 plan commit), not actually one-tap (still starts a recording), no shortcut, menu clutter. Reverted. Do not re-propose. | plan 2026-09 |
| I11 | `[code]` Stall watchdog on the Gemini chat stream + "Searching the web…" label while grounding runs | 1 `chatStopped` on 2026-09-16 (`reason: user`, `partialChars: 0`, `gapMs: 138671`, `dropped: 1`): a grounded `gemini-3.8-flash` reply on a screenshot question ran 3 Google-Search rounds (`total` 10k→24k→46k→78k tokens, `output` 41→106 = tool calls only), then streamed identical usage chunks for 71 s with zero text until the user cancelled at 2 m 19 s. The immediate re-send finished in 36 s. The only stream timeout was the 300 s socket `resourceTimeout`, which keep-alive chunks never trip. | shipped | — | Shipped 2026-09-16 on `feat/gemini-stall-watchdog` (interactive session, not a routine). No progress (text delta / function-call part / growing `totalTokenCount`) for 45 s mid-stream or 90 s before the first chunk → data task cancelled; nothing on screen → one automatic re-send, second stall → error popup; text already shown → partial finalized with `finishReason=STALL`. Log gains `tools=` (`toolUsePromptTokenCount`); UI shows "Searching the web…" next to the typing dots while tool tokens grow before the first delta. **Falsifier:** over the next 30 days of local JSONL after the rebuild, no `chatStopped` signal with `mode: geminiChat`, `partialChars: 0` and `gapMs > 60000` — the watchdog must have fired first (log line `GEMINI-CHAT-STREAM: no progress for …s`). Baseline: 1 such signal in the 2026-09-16 window. Blind spot: a stall the user tolerates below 45 s is invisible by design. **Filed as I6 on 2026-09-16 against origin/main, which lacked usage-review I6 (09-07, uncommitted until 2026-09-19); renumbered I11 at the rebase — the L10 case.** |

## Run log

| Run | Date | Window reviewed | Signals seen | Entries added |
| --- | ---- | --------------- | ------------ | ------------- |
| 1 | 2026-08-03 | 2026-07-27 → 2026-08-02 (1109 interactions) | 28 (all `pasted`; signal stream only live ~1.5h on 08-02) | I1 |
| 2 | 2026-08-17 | 2026-08-10 → 2026-08-13 (690 interactions) | 588 (585 `pasted`, 2 `cancelledWhileProcessing`, 1 `dictationRestart`) | I2, I3 |
| 3 | 2026-08-18 | 2026-08-11 → 2026-08-18 (554 interactions; 08-11–08-13 overlap Run 2's window, only 08-18 is new data) | 455 (453 `pasted`, 2 `cancelledWhileProcessing`, 0 `dictationRestart`) | _(none — see digest: 1 new `cancelledWhileProcessing` on 08-18, gapMs 766397, reinforces I2 but doesn't independently clear the bar for a new entry)_ |
| 4 | 2026-09-07 | 2026-08-31 → 2026-09-07 (1093 interactions) | 1154 (951 `pasted`, 182 `noSpeechDetected`, 10 `cancelledWhileProcessing`, 5 `dictationRestart`, 2 `chatRetry`, 2 `promptRetry`, 1 `chatStopped`, 1 `promptNoSelection`, 0 `requestTimedOut`) | I4, I6, I7 |
| 5 | 2026-09-14 | 2026-09-07 → 2026-09-14 (535 interactions) | 571 (483 `pasted`, 80 `noSpeechDetected`, 5 `cancelledWhileProcessing`, 3 `requestTimedOut`, 0 `dictationRestart`, 0 chat/prompt negatives) | I8, I9 |
