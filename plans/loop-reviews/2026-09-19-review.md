VERDICT: Loops measure well, deliver little — 2 PRs in 7 weeks; finding→build path broken in 3 silent places.

# Agent-loops meta-review — 2026-09-19 (run 2, interactive)

Triggered by the operator's question „die Routinen bringen rein gar nichts" — so this run answers
that first, then does the skill's §1–§5. Interactive run; Fable advisor consulted before writing.

## 0. The answer: what the loops have actually produced, per loop

Seven weeks of machinery (2026-08-01 → 09-19). "Reached you live" means a change running in the
app you use or an action taken in the store, not a ledger row.

| Loop | Runs | Reached you live | What you would lose if it were switched off today |
| ---- | ---- | ---------------- | -------------------------------------------------- |
| **usage-review** | 5 full / 7 Mondays (2 FAILED: budget 08-17, weekly limit 08-24; 08-10 no trace) | **2 bug fixes**: I1 recorder-off-main-thread (shipped same day, by hand) · I2 60 s NetworkDeadline (implementer PR #45; confirmed live 09-14, fired 3×, all recovered) | Two real bugs a week apart that nobody had reported. Cost: 7 of its 9 rows are unbuilt (I3 HOLD 30 d, I4/I8 instrumentation misfiled ASK, I6 BUILD but invisible, I7/I9 DEFECT) — and **I4–I9 plus gaps #9/#10 exist only in the uncommitted working copy** (last ledger commit 09-05) |
| **model-audit** | 3 (08-02, 08-03, 09-03) | **1 migration**: gemini-3.8-flash default (implementer PR #55; build agent timed out, finished by hand: `11dcda7`, `1913620`) | One useful lineup change. The other 6 recommendations unapplied; "transcription default → gpt-transcribe" re-proposed **three audits running** without ever being accepted or rejected — that is the re-proposal noise the ledger rules exist to stop |
| **growth-review** | 2 full (08-18 by hand, 09-05) + **G3 today broken** (253-byte stub mailed as success) | G1 redirect+CTAs → graded **SHIPPED+NO EFFECT** (3 web-referrer views vs ≥45). **The price raise €3.99→€14.99 — your decision on 08-18, informed by G1's numbers — is the single most valuable outcome of the whole system** (+226 % per sale, revenue flat on half the traffic). G2's keyword rewrite: **not acted on in 14 days** | The numbers. Both real growth moves (price, keywords) were found by it; one you took, one is still on the desk. Its recommendations themselves: 0 worked / 1 written off |
| **sales scout + poster** | scout 9 (7 "quiet day"), poster **28 runs, 0 posts** | Nothing. S-001 you posted by hand; S-002 sits PREVIEWED since 08-31. Reddit 429 on every run since 08-22, never fixed | Nothing — `SALES_POST_ENABLED` was never set, so the poster has been a daily no-op mail for a month |
| **implementer** | 294 ticks since 09-03 · 4 builds (q1 clean, q4 timeout→hand-finished, q10 BLOCKED twice ×2 → built by hand as PR #72) | 2 PRs of the 30 merged since 07-20 | The one clean run (I2) proves the path works. But `merge-windows/` **does not exist** — the unattended merge has never once been exercised — and `plans/implementer-log.md` has **1 row for 4 runs** |
| **architect (this)** | **0 scheduled** (09-06 swallowed by the 20-day gate); 1 interactive (08-20) | L1/L3/L4 worked | Nothing scheduled ever ran |

**Cost side:** 88 tooling commits vs 120 app commits since 08-01; +13 k lines under `scripts/ plans/
.agents/ .cursor/` vs +16.6 k under `WhisperShortcut/`. G2's own effort count: ~28 % loop plumbing,
~1 % funnel. Roughly 50 loop mails in 7 weeks; the ones that changed anything are countable on one hand.

**So the honest shape is:** the loops are good at *measuring* — every real product/business move of
these 7 weeks (two bug fixes, the price, the keyword lever, the 3.8 default) started from a loop
number. They are bad at *delivering*: the autonomous finding→build path has produced 2 PRs, and this
audit found it broken in three places, all silent (§2).

## 1. Grade of the previous run (2026-08-20 + 09-12 convergence pass)

| ID | Verdict | Evidence |
| -- | ------- | -------- |
| L2 recoverable killed runs | **NOT APPLIED (2nd time) → superseded** | `run-implementer.sh` still dies on an existing worktree: 13 `worktree dir already exists` (7 consecutive ticks 09-06, 6 on 09-17). The fail-streak net from PR #62 **did** mail on 09-17 03:26, so the silence half is closed; the die-site half is now queue row 12 (ASK). Per ledger rule, L2 is dropped in favour of row 12 |
| L5 missed-fire watchdog | **NOT APPLIED** | No commit. Since then: usage 08-10 still the only fully silent miss; growth 08-22 has no log line either |
| L6 tighten §2 (implementer as first-class loop, business/ paths, cadence↔plist) | **NOT APPLIED** in the skill file — but this digest does all three by hand. Plist check: model-audit doc says "Wed 09:17", plist is `Day=3 06:12`; growth doc "Sat 09:07" matches; both still drift in `agent-loops.md` |
| L7 mandatory Weglassen | **TOO EARLY** | Applied 09-12. Usage 09-14 has it; growth 09-19 is a stub; model-audit and sales have not run since. 1 of 4 — grade at the next run |
| L8 falsifier bookkeeping | **NOT APPLIED** (design not started) | Meanwhile sabaki's register has 108 markers, 2 graded PASS, 0 FAIL, machine-clocked |
| L9 WAITING status | **NOT APPLIED** | No row has needed it yet — 2-month drop clock runs to 2026-11-12 |

Deploy-gate discipline held: G1 was confirmed live before its NO EFFECT; I6 graded "TOO EARLY, no
exposure (0/450 offline)"; I2 graded from live `requestTimedOut` records. No false write-off found.

## 2. Five silences — the loud-failure convention failed on its own terms

`plans/agent-loops.md:76` says silence must never be distinguishable from a quiet week. This audit
found five cases where it was, none of them mailed:

1. **G3 mailed a stub as success.** `growth-review-job.sh:220` fails only on `[ ! -f "$DIGEST" ]`;
   the model wrote the provisional file first (as the prompt tells it to), spawned an ASC delegate,
   and `claude -p` killed the run at the 600 s background ceiling (`CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS`
   is set nowhere; `scripts/loop-prompt.sh` has no "do not delegate" rule, while `AGENTS.md`'s
   model-tiering section — ported 09-17 — tells every session to delegate). Result: a 253-byte digest,
   no G3 ledger entry, no proposal file, subject line "Nothing to do". 09-05 worked because it did not
   delegate.
2. **Queue row 6 is unpickable.** `run-implementer.sh:211-213` splits the row on every `|`; row 6's
   falsifier contains `\|`, so awk sees `NF=11` and `$6`/`$7` are fragments of the falsifier. Promoted
   to BUILD 09-09 00:23; the same tick and **every tick since (174, through 09-19 10:21)** printed "no BUILD/OPEN row".
3. **Two weeks of usage-review output is uncommitted.** I4–I9, gaps #9/#10: working copy only. The
   groomer reads `origin/main`, finds no OPEN gap #9/#10, and filed both instrumentation proposals
   (I4→row 5, I8→row 8) as `ASK` — the auto lane that exists precisely for them never saw them. One
   `git checkout -- plans/` deletes the rows. Parent repo: `business/growth-reviews/2026-09-05` and
   `-09-19` are untracked too.
4. **The merge window has never opened.** `~/.local/state/whispershortcut-implementer/merge-windows/`
   does not exist; q4's PR #55 was merged by hand. Rung 2's "silence merges" is untested code.
5. **`plans/implementer-log.md` has 1 row for 4 runs** — the demotion falsifier (≥2 of 5 structural
   rework) cannot be graded from it. On the log's own terms q4 (timeout, hand-finished) and q10 ×2
   (BLOCKED twice) would be 3 non-clean of 4.

Related, not silent but unaddressed: `shared main could not be fast-forwarded to origin/main` on all 33
ticks since 09-16 20:47 (ssh to github.com failing) — the build lane has run on a stale base for 3 days.

## 3. Funnel table (counts)

| Loop | Proposed | Shipped | Measured | Written off | Hit rate | Median deploy lag (acted-on) |
| ---- | -------- | ------- | -------- | ----------- | -------- | --------------------------- |
| usage | 9 (I1–I9; I5 was a plan, not a loop row) | 2 | 2 worked (I1 by absence, I2 live) | 0 | 2/2 — *not reaching*: every built row was the obvious one | I1 0 d · I2 2 d · rest ∞ (12–30 d and counting) |
| model | ~7 recs over 3 audits | 1 | 1 (3.8 default, latency settled 09-08) | 0 | 1/1 — same caveat | 5 d |
| growth | 2 recs | 1 (G1) | 1 | 1 (G1 NO EFFECT) | **0/1** | G1 0 d · G2 ∞ (14 d) |
| sales | 2 drafts | 0 posted by machine | — | — | n/a | — |
| implementer | 4 runs | 2 PRs | — | 2 BLOCKED-twice | 1 clean / 4 | Flag→PR 1 d (q1), 2 d (q4) |
| architect | 9 | 4 applied | 3 worked | 0 | 3/3 | L1 0 d · L7 23 d · 5 not applied |

Reading: the hit rates near 100 % on usage/model are the skill's "only proposes the obvious" case,
because only the obvious rows ever get built. Growth is 0 for 1 — one honest write-off. Nothing
below the top row of any ledger has been graded.

## 4. Blind spots (`plans/instrumentation-gaps.md`)

Rows #1–#8 verified unchanged. #9 and #10 are correct but **exist only in the working copy** (§2.3) — no
status edit this run; committing them is the fix. New meta gap, tracked as L10–L12 rather than as a
product row: the loops cannot see their own hand-over failures (§2). Gap fixes ranked above features:
held in prose (usage run 4/5 ranked I4/I8 first) but not in effect, because the groomer never saw them.

## 5. Best-practice findings

| Finding | Adopt/adapt/ignore | Touches | Effort | Falsifier |
| ------- | ------------------ | ------- | ------ | --------- |
| `claude -p` kills background agents at 600 s (CLI behaviour, `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS`); scheduled prompts must either forbid delegates or raise the ceiling | **adopt** (L11) | `scripts/loop-prompt.sh`, all four job scripts | S | next growth run with `asc` slow completes with a full digest |
| Harness essay (Anthropic 2026-03-24): "separating the agent doing the work from the agent judging it" — already ours | ignore | — | — | — |
| Regimes (arXiv 2606.10241): promote/discard decisions must live in the agent's own replayable history, not a side DB | adapt — our side DB is the uncommitted working copy (§2.3) | usage/growth job commit step | S | L10 |
| Evolve-run pattern (aibuilderclub 2026-07): "every win was a removal, a narrowing, or a mechanical step turned into code — not one evolve pass produced a novel capability" | adopt as expectation for this loop: Weglassen is the product, not a footnote | this skill | — | this digest's Weglassen is acted on |
| Agent Skills spec: `name` must match directory name, `description` ≤1024 chars | ignore — conforms | — | — | — |
| Claude Code 2.1.277: subagent results now arrive under a marked header (cannot pass as instructions) | watching | — | — | — |

## 6. Transfer

### Import (from `~/sabaki.dance.v3`, commits since 2026-09-12)

- **ASK-lane judge** (`3873cd561`, 09-13): Opus routes ASK rows build/reject/escalate; only migration, deletion, outbound send reach the owner; "silence decides nothing"; a row is never asked twice. Here: rows 5, 7, 8, 9, 12 sit ASK with nobody scheduled to look. → candidate for a future L-row once L10–L12 hold (it would move work *into* a lane that is currently broken).
- **Row-fail limit + flaky re-run** (`dae9d81f7`): second failed run on the same row → FAILED, lane moves on. Here q10 was built twice on consecutive days with the same BLOCKED plan — this is queue row 12 already.
- **"compare the status WORD, never the noted cell"** (`704563571`): same defect class as our `\|` row-6 bug, fixed there 09-13. → L12.
- **Build cache staged before review** (`4e91080a8`): "49 Claude minutes for nothing" when the review hung. Our q4 lost its build the same way. Later.
- **Falsifier register** (`docs/falsifier-state.json`, 108 markers, `falsifier-due.ts` daily): L8, still the largest gap between the repos.
- **Divergence table is stale**: sabaki no longer pastes ledgers by hand — `append-ledgers.ts` commits them from the Mac tick (README:241-246). Our "Ledger writes" row should say so. Sabaki's meta-loop skill has **no** cross-repo read of this repo; the "symmetric" claim in `agent-loops.md:317` is one-directional.
- Sabaki's own numbers for the same question: elon-log 2026-08-18 "Loop-/Agenten-Maschinerie ~25 %, kein einziger Commit adressiert 'ein Organizer zahlt Geld'"; 2026-09-06 stop-doing: "Weitere Plattform-Maschinerie bauen — ausdrücklich einschließlich der Loop-/Implementer-Infrastruktur". Same diagnosis, both repos.

### Export → paste into `~/sabaki.dance.v3/docs/loop-meta-log.md`

```markdown
## 2026-09-19 (whisper-shortcut, Architect run 2 — cross-repo lesson)

Whisper's meta-run found five silent hand-over failures in one audit, none mailed, all
inside a system whose first convention is "loud failure":

1. A scheduled `claude -p` run delegated to a subagent and was killed at the 600 s
   background ceiling (`CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS` unset); the job's only
   completeness check was `[ -f digest ]`, so a 253-byte "provisional" stub went out as
   "Nothing to do". Check: does `run-routine.sh` forbid delegates or set the ceiling?
2. A queue row with `\|` inside a table cell was invisible to an awk `-F'|'` picker for
   174 ticks — same class as your 704563571 "status WORD" fix. Lint cells at write time.
3. Loop jobs that write ledgers into the working copy without committing: the groomer
   (reading origin/main) could not see the OPEN gap rows and misfiled two instrumentation
   proposals to ASK. "Delivered = committed" has to bind the loops, not only sessions.
4. The unattended-merge path (merge window) had never once been exercised in 4 weeks.
5. The implementer log had 1 row for 4 runs, so the demotion falsifier was ungradeable.

Numbers for the value question: 7 weeks, 2 autonomous PRs, 88 tooling vs 120 app commits;
the most valuable outcome (price raise) was a human decision informed by a loop's numbers.
```

## Self-answered

- *Is the 08-24 usage run a silent failure?* No — log says FAILED (weekly limit) with a mail, and a digest exists from a re-run the same morning. Loud.
- *Did the fail-streak net (PR #62) work?* Yes — mail at 09-17 03:26 after the second consecutive non-zero tick. Rule applied: a net that fired is not a finding.
- *Should L2 be dropped?* Superseded by queue row 12, which names the same die-site with a falsifier. Reversed if row 12 is rejected.
- *Is the gemini-3.8 migration an implementer success?* Half: the build agent timed out; PR #55 was completed by hand. Counted as 1 PR, not as a clean run.

## Weglassen

**`com.whispershortcut.sales-poster`** — 28 daily runs, 28 "refusing to post", 0 posts, one mail each.
Unload it until `SALES_POST_ENABLED=1` is a decision that has been made; the scout can keep drafting
into `queue.jsonl` without a poster. Second candidate: the model-audit's "carried over, unapplied for
three months" transcription-default recommendation — accept or put it in the rejected table, but stop
carrying it.

## Open questions (would loosen a gate, or are the owner's to make)

1. **Commit the uncommitted loop output?** `plans/improvement-ledger.md`, `plans/instrumentation-gaps.md`
   (submodule) and `business/growth-reviews/2026-09-05`, `-09-19`, `business/growth-ledger.md` (parent)
   are working-copy only. This run did not commit them (not its files). Losing them loses I4–I9.
2. **Keep the growth and sales loops running at all?** The numbers say growth's *measurement* paid
   for itself once (price); its recommendations are 0/1 and the current one has waited 14 days for a
   human hour. Sales has produced nothing the machine posted. This is a priority call about your
   time, not a gate — the skill cannot answer it.
3. L2 drop and the `$(NF-3)`-style quick fix for the picker were **not** applied: interactive rule,
   edits to job scripts only on approval in the session.
