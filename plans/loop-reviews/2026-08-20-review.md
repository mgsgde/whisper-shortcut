VERDICT: Implementer closed execution gap (I2→PR#45 in 2d); silent missed fires + unrecoverable kills remain binding.

# Agent-loops meta-review — 2026-08-20 (run 1, first real run)

**Previous run graded:** Baseline run 0 (2026-08-18) only — no prior meta digest. First real
Architect run; continue after one-line note as the skill requires.

## 1. Grade of open meta-proposals

| ID | Proposal | Verdict | Evidence |
| -- | -------- | ------- | -------- |
| L1 | Port Sabaki rung-2 implementer | **APPLIED+WORKED** | Built 2026-08-18; queue #1 (I2) → [PR #45](https://github.com/mgsgde/whisper-shortcut/pull/45) merged 2026-08-19 with build+tests green, Opus `APPROVE` first pass, Outcome `MERGED` clean. Falsifier ("≥1 flagged proposal reaches mergeable PR with green gates") held. |
| L2 | Recoverable killed implementer runs | **NOT APPLIED** | `run-implementer.sh:210` still `die "worktree dir already exists"`. No resume/cleanup path. Workaround (`nohup`) still required. Re-argue once below as L2 notes — second miss next run → drop or re-scope. |
| L3 | Diff against merge-base, not main tip | **APPLIED+WORKED** | Present in runner; run 1 completed without bogus out-of-scope trips from mid-run main commits. |
| L4 | Runner diagnostics → stderr (not stdout captures) | **APPLIED+WORKED** | Run 1 produced a real file count and a clean `APPROVE` parse — the exact failure L4 named did not recur. |

## 2. Per-loop funnel

Hit rate = worked / (worked + written off). None of the product/growth falsifiers have reached a
honest grade date yet → hit rates are **not computable** (not "100%"). That matches the baseline
thesis: too early to celebrate proposal quality.

| Loop | Proposed | Accepted | Shipped/Applied | Measured | Rejected | Hit rate | Did it run? | Deploy lag (acted-on) |
| ---- | -------- | -------- | --------------- | -------- | -------- | -------- | ----------- | --------------------- |
| **usage-review** | 1 open (I3) | 0 | 2 (I1, I2) | 0 | 0 | n/a | **Partial.** Digests: 08-03, 08-17, 08-18. **Mon 08-10 missing** with no FAILED mail (silent skip — Mac asleep / launchd no catch-up). 08-17 budget-killed ($3) then loud FAILED + 08-18 retry. | I1: 0d · I2: **2d** (proposed 08-17 → merged 08-19) |
| **model-audit** | 0 open migrations this month | — | Prior month's 3 recs shipped; 08-03 verdict "nothing dominated" | n/a | 0 | n/a (no falsifier cycle closed as write-off) | Yes — 08-02 + 08-03. Next expected ~Sep 3 (plist Day=3 06:12). | Same-day apply when acted |
| **growth-review** | G1 recommendation acted same day | — | Site `/go/appstore` + price €14.99 + review replies | 0 (falsifier ~08-31) | 0 | n/a | Yes once (08-18). **No `build/logs/growth-review.log`** — look like interactive/manual, not launchd. Next Sat 08-22. | 0d to act; grade **TOO EARLY** |
| **architect (this)** | L2 open; L5–L7 new | — | L1/L3/L4 applied | L1 falsifier held | 0 | n/a | First real run (interactive 08-20). Scheduled: 6th monthly. | — |
| **implementer** | Queue #1 MERGED | — | 1/1 clean merge | demotion falsifier: 0/5 structural rework | 0 | 1 clean / 1 = early | 1 run, Outcome MERGED clean. Kill-switch / empty-queue paths verified at build. | Flag→PR: **1 day** |

### Cost / noise

- Usage 08-18 correctly added **zero** ledger rows (reinforcement of I2 only) — anti-padding held.
- Usage 08-17 budget kill was loud (FAILED mail) — good.
- Growth G1 digest still claims `plans/growth-reviews/` and `plans/growth-ledger.md` though both live under `../business/` — ledger/path drift that will confuse the next growth grade if unfixed (propose under L6, not a product change).

### Deploy-gate discipline

- **I1:** shipped 08-03; no newer `hang-*.txt` naming `ChunkedDictateRecorder` / `LiveMeetingRecorder` after ship (last matches 07-28, 08-02). Verify-by-absence looks good, but leave status as shipped until a usage run explicitly moves it to `measured` — do not invent a write-off.
- **I2:** in `v7.99` / main; local rebuild is the live gate for own-usage falsifier; needs **two** post-ship usage windows — **TOO EARLY**, never `NO EFFECT`.
- **G1:** falsifier ~08-31 — **TOO EARLY**. Price change overlaps page-view falsifier — next growth run must attribute carefully (already noted in ledger).
- No `NO EFFECT` rows found that skipped the deploy gate.

## 3. Blind spots (`plans/instrumentation-gaps.md`)

Verified against reality this run (status updates proposed — not silently rewritten beyond what rung 0 allows; rows below match current file unless noted):

| # | Status check | Notes |
| - | ------------ | ----- |
| 1 | PARTIAL — still correct | Arrivals via Cloud Run logs; exit hop `/go/appstore` built — needs post-deploy confirmation in growth numbers |
| 2 | PARTIAL — still correct | Developer-only feature granularity |
| 3 | OPEN — still correct | No review ingestion between growth runs |
| 4 | PARTIAL — still correct | GitHub 14-day memory |
| 5 | OPEN — still correct | macOS rating count unreadable via `asc` |
| 6 | OPEN — still correct | Apple install/delete/session recipe exists but not wired into Phase 1 of `review-growth` |
| 7 | OPEN/PARTIAL — still correct | Apple `pt` token manual; own-domain attribution PARTIAL via gap #1 |

**New gap (meta, not product):** scheduled jobs can miss a fire with **no digest and no FAILED mail** (usage Mon 08-10). That blinds every loop's "did it run?" check the same way TCC-denied empty reads used to. Tracked as proposal **L5**, not as a product instrumentation row.

Gap fixes were ranked above features in G1 (site counter / campaign path) — discipline held for growth. Usage still has I3 open while I2 was the right BUILD flag.

## 4. Best-practice findings

Sources reached: Claude Code CHANGELOG (raw), agentskills.io/home, Anthropic engineering (effective harnesses for long-running agents), WebSearch on evaluator-optimizer / independent verifier loops. `docs.claude.com` agents overview redirected/errored in fetch — noted, continued.

| Finding | Adopt / adapt / ignore | Touches | Effort | Falsifier |
| ------- | ---------------------- | ------- | ------ | --------- |
| Maker ≠ verifier already implemented (implementer Cursor builds / Opus judges; loops don't self-grade with same context) | **ignore** (already have) | — | — | — |
| Immutable goldens during optimize (evaluator-optimizer literature) | **ignore** (model-audit measurements are separate files; skill forbids editing goldens mid-judge) | — | — | — |
| Decision observability = every edit ships a prediction verified next round (AgentPatterns / Anthropic harness essay: artifacts across sessions) | **adapt** — already our falsifier rule; tighten by grading implementer Outcome column every meta run | `review-agent-loops` §2 | S | Next digest has an implementer funnel row (L6) |
| Claude Code: `--max-budget-usd` now also stops background subagents (2.1.217) | **watching** | jobs already pass the flag | — | Re-check if a future job spawns subagents under budget |
| Claude Code scheduled tasks / hooks / fork subagents | **watching** | we use launchd + `claude -p`, not CC scheduled tasks | — | Only revisit if moving off launchd |
| Agent Skills standard (name + description frontmatter) | **ignore** — skill already conforms | — | — | — |
| Sabaki finding→shipped auto-groomer that sets BUILD | **ignore here / Open question** — would loosen "Flagging is human" | — | — | — |
| Sabaki mandatory **Weglassen** (name something to delete each run) | **adopt** | digest templates for usage/growth/model/meta | S | L7: next scheduled digests each name one deletion candidate |
| Long-running harness: leave clear artifacts for the next session | **adapt** — ledgers already do; missed-fire leaves **no** artifact → L5 | job wrappers | M | See L5 |

## 5. Transfer

### Import (→ proposals)

1. **Missed-fire ≠ quiet week** (our measurement, Sabaki's loud-failure doctrine): L5.
2. **Weglassen every digest** (Sabaki product/meta ledgers): L7.
3. **Do not import** auto-BUILD groomer / merge-veto windows without a human commit — that loosens gates (see Open questions).
4. Sabaki still has GEBAUT+NICHT DEPLOYED as explicit status; we encode the same idea as TOO EARLY + deploy gate — no drift worth fixing.

### Export (paste into `~/sabaki.dance.v3/docs/loop-meta-log.md` — do not write there from this run)

```markdown
## 2026-08-20 — import from whisper-shortcut (Architect run 1)

**Verdikt:** Execution gap closed on first flagged row (I2 hang → PR #45, 2 days, clean MERGED);
binding residual risks are silent missed launchd fires and unrecoverable killed implementer worktrees.

**Lessons worth stealing:**

1. **Stdout-capture vs logging (L3/L4).** Never `$(gate)`-capture a helper that also `echo`s
   diagnostics on stdout — verdict strings and file lists get poisoned. Route `log`/`warn` to
   stderr; diff scope against `merge-base`, not `main`'s tip, when main can move mid-run.
2. **Subscription-only guarantee.** Unset `ANTHROPIC_API_KEY` / `ANTHROPIC_AUTH_TOKEN` at the
   top of every scheduled job so a shell-profile key cannot silently flip Max → per-token billing.
3. **Business data out of the public app repo.** Growth/usage digests that quote revenue or
   transcripts live in a private sibling (`business/`), not in the public submodule — same
   auditability, no accidental publish.
4. **Implementer test gate off metered keys.** Skip `(live)` suites in the unattended runner
   (`IMPLEMENTER_LIVE_TESTS=0`); keep live roundtrips for interactive `run-tests.sh` only.
5. **Anti-padding held.** A usage week that only reinforces an open row must add zero ledger
   entries (WhisperShortcut usage run 3) — "quiet week" is a valid verdict.

**Zur Lockerung vorgeschlagen (Mensch entscheidet):** —
```

## 6. Skill meta-audit (is `review-agent-loops` itself still correct?)

**Overall:** Still aligned with best practices (tighten-only, grade-first, maker≠judge, max 3
proposals, Sabaki read-only exchange). Not stale in architecture.

**Broken / drifting references:**

| Issue | Severity |
| ----- | -------- |
| §2 lists "usage-review, model-audit, growth-review, and this one" — **omits implementer**, which `plans/agent-loops.md` treats as a first-class loop | High — meta-blind to the only rung-2 capability |
| Skill never tells the run to read `../business/usage-reviews/` (digests moved out of `plans/`; jobs write there) | Medium — funnel "did it run?" incomplete if agent only greps `plans/` |
| Parent `AGENTS.md` claims `whisper-shortcut/.cursor/skills/` — **directory does not exist**; real path is `.cursor/skills/` (`.claude/skills` → symlink) | Medium for agents following parent docs; skill path in job script is correct |
| `plans/agent-loops.md` cadence "Wed 09:17 monthly" for model-audit **≠** plist `Day=3 Hour=6 Minute=12` | Low — docs drift, not skill body |
| Growth ledger/digest still cite `plans/growth-reviews/` after the business/ move | Low — growth artifact drift; meta skill should catch it when measuring growth |

**Still correct:** Sabaki paths, tighten-only rule, ledger/digest write set, agentskills frontmatter,
`plans/instrumentation-gaps.md` register, Claude changelog + agentskills + WebSearch sweep list.

## Open questions (would loosen a gate — human only)

1. Sabaki's finding→shipped design auto-sets `BUILD` / merge-veto windows. Adopting that here would
   weaken "Flagging is human; no agent sets BUILD." Only a human commit may consider it.
2. Raising usage-review `--max-budget-usd` above $3 after the 08-17 kill is more capacity, not a
   looser evidence bar — still a human cost decision; not proposed as a meta gate change.
3. Dropping L2 after one more NOT APPLIED if `nohup` remains the standing workaround — human call.

## Proposed ledger deltas (applied in `plans/loop-ledger.md` this run)

- L1 → `measured` (falsifier held)
- L2 → notes: NOT APPLIED, first re-argue
- L3, L4 → confirm `applied` / worked
- Add **L5, L6, L7** (below)
- Run log row 1
